import './Stars.scss';
import ScrollableFeed from 'react-scrollable-feed';

@inject('userStore', 'channelStore') export default class Stars extends Component {
  start() {
    // this.props.userStore.myStars.forEach((starMessage) => {
    //   console.log(starMessage)
    //   console.log(starMessage.sender)
    // })
  }
}